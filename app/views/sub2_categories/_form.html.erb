<%= form_for(sub2_category) do |f| %>
  <% if sub2_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub2_category.errors.count, "error") %> prohibited this sub2_category from being saved:</h2>

      <ul>
      <% sub2_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>

  <div class="field">
    <%= f.label :sub1_category_id %>
    <%= f.text_field :sub1_category_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
